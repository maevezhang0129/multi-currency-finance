# 复制为 .env.local 后填入真实值。.env.local 已被 .gitignore 忽略，永远不要提交。
#
# 两个连接串都能在 Supabase 控制台 Project Settings → Database → Connection string
# 找到。它们不是同一条，用途不同，见下方说明。

# 运行时连接串：Supavisor transaction pooler，端口 6543。
# serverless（Vercel）必须走它——函数实例会频繁创建销毁，直连会打满 Postgres 连接数。
# 注意用户名形如 postgres.<project-ref>，不是裸的 postgres。
DATABASE_URL="postgresql://postgres.your-project-ref:YOUR-PASSWORD@aws-0-your-region.pooler.supabase.com:6543/postgres"

# 迁移连接串：Session pooler 或 Direct connection。只在本地跑 drizzle-kit 时使用，
# 不会被应用运行时读取。
#
# 从控制台 Connect 页面原样复制，不要拿上面那条改端口——不同项目的 session
# 模式主机名不一定相同，推导出来的串会连不上。
#
# Direct connection（db.<ref>.supabase.co:5432）默认只有 IPv6；本机没有全局
# IPv6 地址的话用 Session pooler 那条（IPv4，全 tier 可用）。
DIRECT_DATABASE_URL="从 Supabase 控制台 Connect 页面复制 Session pooler 那条"
